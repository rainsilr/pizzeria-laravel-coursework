# Пиццерия Laravel

Учебное веб-приложение для курсовой работы по ПМ.09 «Проектирование, разработка и оптимизация веб-приложений».

## Описание

Проект реализует сайт пиццерии с публичным просмотром меню и защищенным управлением позициями меню после авторизации.

Основная сущность проекта: пицца.
Связанная сущность: категория пиццы.

## Технологии

- PHP 8.3
- Laravel 13
- MySQL
- Blade
- Laravel Breeze
- Eloquent ORM
- Vite и Tailwind CSS

## Возможности

- регистрация и вход пользователей через Laravel Breeze;
- публичная главная страница;
- публичный список пицц с выводом связанных категорий;
- детальная страница пиццы;
- создание, редактирование и удаление пицц только для авторизованных пользователей;
- серверная валидация форм;
- поиск по названию и описанию;
- фильтрация по категории;
- пагинация списка;
- flash-сообщения после операций;
- заполнение базы тестовыми данными через сидер.

## Локальное развертывание

1. Установить зависимости PHP:

```bash
composer install
```

2. Установить зависимости Node.js:

```bash
npm install
```

3. Создать файл окружения:

```bash
cp .env.example .env
```

4. Сгенерировать ключ приложения:

```bash
php artisan key:generate
```

5. Создать базу данных MySQL `pizzeria_laravel` и настроить подключение в `.env`:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=pizzeria_laravel
DB_USERNAME=root
DB_PASSWORD=
```

6. Выполнить миграции и заполнить базу тестовыми данными:

```bash
php artisan migrate --seed
```

7. Собрать фронтенд:

```bash
npm run build
```

8. Запустить приложение:

```bash
php artisan serve
```

После запуска приложение будет доступно по адресу `http://127.0.0.1:8000`.

## Тестовый пользователь

Сидер создает администратора:

- email: `admin@example.com`
- пароль: `password`
